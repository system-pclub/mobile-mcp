<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:description="@string/app_desc"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MCPDemo">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Register externally exposed service -->
        <service
            android:name=".CommandGatewayService"
            android:enabled="true"
            android:exported="true"
            tools:ignore="ExportedService">

            <!-- App-level metadata -->
            <meta-data
                android:name="mcp.application.name"
                android:value="Daily Clock-in" />
            <meta-data
                android:name="mcp.application.description"
                android:value="@string/app_desc" />

            <!-- Tool-level metadata -->
            <meta-data
                android:name="mcp.capabilities"
                android:resource="@xml/mcp_capabilities" />

            <intent-filter>
                <action android:name="com.example.mcpdemo.COMMAND_GATEWAY" />
            </intent-filter>
        </service>

    </application>

</manifest>
