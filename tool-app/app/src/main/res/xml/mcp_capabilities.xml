<?xml version="1.0" encoding="utf-8"?>
<mcp>
    <capability
        id="check_in_today"
        description="Check in today."
        version="1" />
    <capability
        id="query_check_in"
        description="Query whether a specific date has been checked in."
        version="1">
        <input>
            <param name="date" type="string" required="true" description="Date to query in format yyyy-MM-dd" />
        </input>
        <output>
            <param name="date" type="string" description="The queried date" />
            <param name="has_checked_in" type="boolean" description="Whether the date has been checked in" />
        </output>
    </capability>
    <capability
        id="make_up_check_in"
        description="Make up a check-in for a specific date."
        version="1">
        <input>
            <param name="date" type="string" required="true" description="Date to make up check-in in format yyyy-MM-dd" />
        </input>
        <output>
            <param name="date" type="string" description="The date that was checked in" />
            <param name="success" type="boolean" description="Whether the make-up check-in was successful" />
        </output>
    </capability>
</mcp>
